![Foto do comando](cybersec-doc-perms/blob/main/verificar.png)


# 🔍 Verificando Permissões no Linux (`ls -l`)

## 📌 O que é?
O comando `ls -l` é utilizado para listar arquivos e diretórios, exibindo suas permissões, dono e grupo.

## 🔹 Exemplo de Uso
```bash
ls -l arquivo.txt
